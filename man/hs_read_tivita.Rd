% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-tivita.R
\name{hs_read_tivita}
\alias{hs_read_tivita}
\title{Read a TIVITA Suite Recording}
\usage{
hs_read_tivita(path, bands = NULL, verbose = TRUE)
}
\arguments{
\item{path}{Path to a \verb{*_SpecCube.dat} file.}

\item{bands}{Optional integer vector of band indices to read. \code{NULL}
(default) reads all bands.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
An \link{hsi_cube} object. Values are calibrated reflectance, normally
within \verb{[0, 1]} but not clamped, since specular regions legitimately
exceed it.
}
\description{
Reads a Diaspective Vision TIVITA \verb{*_SpecCube.dat} recording through the
\pkg{tivis.r} package and returns it as an \link{hsi_cube}.
}
\details{
Unlike the Cubert reader, this needs no vendor SDK: the TIVITA container is
a plain binary format and \pkg{tivis.r} is pure R.

Acquisition metadata from the Suite's \verb{*_meta.log} is attached to the cube
when present, along with the paths of the parameter images the Suite
exported beside the recording (RGB rendering, oxygenation, NIR perfusion,
THI, TWI). Those are the Suite's own results and are useful as a reference
when checking independently computed indices such as \code{\link[=hs_sto2]{hs_sto2()}}.
}
\examples{
\donttest{
# Requires the tivis.r package
# cube <- hs_read_tivita("2019_11_25_13_29_24_SpecCube.dat")
}

}
\seealso{
\code{\link[=hs_read_cube]{hs_read_cube()}} for extension-based dispatch,
\code{\link[=hs_read_cubert]{hs_read_cubert()}} for Cubert session files.
}
