% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify.R
\name{hs_classify_rf}
\alias{hs_classify_rf}
\title{Random Forest Pixel Classification}
\usage{
hs_classify_rf(
  cube,
  training_labels,
  training_mask = NULL,
  num.trees = 500L,
  mtry = NULL,
  ...
)
}
\arguments{
\item{cube}{An \link{hsi_cube} object.}

\item{training_labels}{Character or factor matrix with class labels.
\code{NA} for unlabeled pixels.}

\item{training_mask}{Logical matrix. Default \code{NULL}.}

\item{num.trees}{Integer. Number of trees. Default \code{500}.}

\item{mtry}{Integer. Variables per split. Default \code{NULL} (auto).}

\item{...}{Additional arguments passed to \code{\link[ranger:ranger]{ranger::ranger()}}.}
}
\value{
A list with class \code{"hsi_classification"} containing \code{class_map}.
}
\description{
Trains a random forest on labeled pixels and classifies all pixels.
Requires the \code{ranger} package.
}
\examples{
\donttest{
# Requires ranger package
cube <- hs_example_cube()
labels <- matrix(NA_character_, 30, 30)
labels[1:10, 1:10] <- "class_a"
labels[20:30, 20:30] <- "class_b"
if (requireNamespace("ranger", quietly = TRUE)) {
  result <- hs_classify_rf(cube, labels)
}
}

}
