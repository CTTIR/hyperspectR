% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluate.R
\name{hs_grouped_evaluate}
\alias{hs_grouped_evaluate}
\title{Evaluate Classification with Entire Groups Held Out}
\usage{
hs_grouped_evaluate(
  spectra,
  wavelengths,
  labels,
  groups,
  folds = NULL,
  n_folds = 5L,
  method = c("svm", "rf"),
  recipe = hs_recipe(),
  model_args = list(),
  seed = 1L,
  bootstrap = 1000L
)
}
\arguments{
\item{spectra}{Numeric matrix, observations in rows and wavelengths in columns.}

\item{wavelengths}{Numeric band centers in nm.}

\item{labels}{Character class labels for each observation.}

\item{groups}{Independent group identifiers, usually subject identifiers.}

\item{folds}{Optional named integer vector mapping every group to a fold.}

\item{n_folds}{Number of folds when no mapping is supplied. Default 5.}

\item{method}{Classifier, \code{"svm"} or \code{"rf"}.}

\item{recipe}{Processing recipe. Learned references are fitted inside each fold.}

\item{model_args}{Named list of fixed classifier parameters.}

\item{seed}{Random seed for fold assignment and model fitting.}

\item{bootstrap}{Number of group bootstrap samples for the accuracy interval.}
}
\value{
List of out-of-fold predictions, group metrics, confusion matrix,
group-mean accuracy and interval, fold assignments and fitted recipes.
}
\description{
Splits by subject (or another independent sampling unit), fits preprocessing
only on training spectra, and predicts held-out groups. Hyperparameters must
be chosen before evaluation; this function does not tune on validation data.
Group accuracy is conditional on valid predictions; prediction coverage and
unevaluated group counts are reported separately. Per-class recall treats
withheld predictions as missed labels. Accuracy intervals resample whole
groups, not pixels. These intervals summarize
out-of-fold predictions and do not include uncertainty from refitting models.
}
