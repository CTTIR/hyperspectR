% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibrate.R
\name{hs_calibrate}
\alias{hs_calibrate}
\title{Calibrate Raw HSI Data to Reflectance}
\usage{
hs_calibrate(cube, dark, white, clamp = TRUE)
}
\arguments{
\item{cube}{An \link{hsi_cube} object with raw data.}

\item{dark}{An \link{hsi_cube} object or 3D array representing the dark reference
(lens cap measurement).}

\item{white}{An \link{hsi_cube} object or 3D array representing the white
reference (Spectralon panel measurement).}

\item{clamp}{Logical. Clamp output to \verb{[0, 1]}. Default \code{TRUE}.}
}
\value{
An \link{hsi_cube} object with reflectance values.
}
\description{
Applies dark current subtraction and white reference normalization:
\code{R(x,y,lambda) = (raw - dark) / (white - dark)}. Values are clamped to
\verb{[0, 1]} unless \code{clamp = FALSE}.
}
\examples{
cube <- hs_simulate_cube(rows = 10, cols = 10, noise_sd = 0)
dark <- hsi_cube(array(0.01, dim(cube$data)), cube$wavelengths)
white <- hsi_cube(array(0.95, dim(cube$data)), cube$wavelengths)
cal <- hs_calibrate(cube, dark, white)
range(cal$data)

}
