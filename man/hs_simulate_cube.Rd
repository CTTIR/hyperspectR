% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{hs_simulate_cube}
\alias{hs_simulate_cube}
\title{Generate a Synthetic HSI Cube for Testing and Examples}
\usage{
hs_simulate_cube(
  rows = 50L,
  cols = 50L,
  wavelengths = seq(430, 910, by = 8),
  n_regions = 4L,
  sto2_range = c(0.3, 0.95),
  noise_sd = 0.01,
  seed = 42L
)
}
\arguments{
\item{rows}{Integer. Spatial rows. Default \code{50}.}

\item{cols}{Integer. Spatial columns. Default \code{50}.}

\item{wavelengths}{Numeric vector. Wavelength grid in nm.
Default: \code{seq(430, 910, by = 8)} matching Cubert Ultris X MR.}

\item{n_regions}{Integer. Number of distinct tissue regions. Default \code{4}.}

\item{sto2_range}{Numeric vector of length 2. Range of StO2 values (0 to 1).
Default \code{c(0.3, 0.95)}.}

\item{noise_sd}{Numeric. Gaussian noise standard deviation. Default \code{0.01}.}

\item{seed}{Integer. Random seed for reproducibility. Default \code{42}.}
}
\value{
An \link{hsi_cube} object with metadata including ground-truth \code{region_map}.
}
\description{
Creates a realistic synthetic tissue hyperspectral cube with known ground
truth. Simulates a tissue scene with regions of varying oxygenation,
hemoglobin concentration, and a background (non-tissue) region.
}
\examples{
cube <- hs_simulate_cube(rows = 20, cols = 20)
dim(cube)

}
