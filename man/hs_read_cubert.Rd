% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-cubert.R
\name{hs_read_cubert}
\alias{hs_read_cubert}
\title{Read a Cubert .cu3s Session File}
\usage{
hs_read_cubert(
  path,
  index = 1L,
  mode = c("reflectance", "spectral_radiance", "dark_subtract", "raw"),
  settings_dir = NULL,
  verbose = TRUE
)
}
\arguments{
\item{path}{Path to \code{.cu3s} Cubert session file.}

\item{index}{Integer. Measurement index within session (1-based). Default \code{1}.}

\item{mode}{Character. Processing mode: \code{"reflectance"} (default),
\code{"spectral_radiance"}, \code{"dark_subtract"}, \code{"raw"}.}

\item{settings_dir}{Character or \code{NULL}. Path to the CUVIS settings
directory (e.g., \code{"C:/ProgramData/cuvis"}). If \code{NULL} (default), uses
the \code{CUVIS_SETTINGS} environment variable or a temporary directory.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
An \link{hsi_cube} object. Reflectance data is in 0-1 range.
}
\description{
Reads Cubert session files using the \code{cuvis.r} package, an R wrapper
for the Cubert CUVIS C SDK
(see \url{https://github.com/r-heller/cuvis.r}).
}
\details{
The processing context automatically loads dark/white references embedded
in the session file, so separate reference files are typically not needed.

Cubert reflectance values are stored as uint16 scaled by 10000
(i.e. 10000 = 100\% reflectance). This function automatically converts
to fractional reflectance (0-1) when \code{mode = "reflectance"}.
}
\examples{
\donttest{
# Requires cuvis.r package and Cubert CUVIS SDK
# cube <- hs_read_cubert("path/to/session.cu3s")
}

}
