% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-read.R
\name{hs_read_envi}
\alias{hs_read_envi}
\title{Read an ENVI Hyperspectral Image File}
\usage{
hs_read_envi(
  path,
  backend = "auto",
  bands = NULL,
  extent = NULL,
  verbose = TRUE
)
}
\arguments{
\item{path}{Path to the ENVI header file (.hdr) or binary file.}

\item{backend}{Character. \code{"auto"} (default) uses terra if available,
otherwise falls back to built-in reader. \code{"builtin"} forces pure-R reader.
\code{"terra"} forces terra (errors if not installed).}

\item{bands}{Integer vector of band indices to read. Default \code{NULL} = all.}

\item{extent}{Numeric vector \code{c(row_start, row_end, col_start, col_end)} for
spatial subset. Default \code{NULL} = full image.}

\item{verbose}{Logical. Print progress messages. Default \code{TRUE}.}
}
\value{
An \link{hsi_cube} object.
}
\description{
Reads ENVI format hyperspectral data consisting of a \code{.hdr} header file
and a binary data file. Supports BSQ, BIL, and BIP interleave formats.
}
\examples{
hdr_path <- hs_example_files()
cube <- hs_read_envi(hdr_path, verbose = FALSE)
dim(cube)

}
