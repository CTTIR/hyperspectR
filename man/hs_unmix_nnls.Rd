% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unmix.R
\name{hs_unmix_nnls}
\alias{hs_unmix_nnls}
\title{Linear Spectral Unmixing via NNLS}
\usage{
hs_unmix_nnls(
  cube,
  endmembers,
  sum_to_one = FALSE,
  backend = c("builtin", "nnls"),
  keep_residuals = TRUE,
  chunk_size = 10000L
)
}
\arguments{
\item{cube}{An \link{hsi_cube} object (reflectance or absorbance).}

\item{endmembers}{Numeric matrix. Columns = endmember spectra. Rows = bands.
Column names become abundance map labels.}

\item{sum_to_one}{Logical. Apply sum-to-one constraint. Default \code{FALSE}.}

\item{backend}{Explicit solver: \code{"builtin"} (default) or \code{"nnls"}.}

\item{keep_residuals}{Logical. Retain the full residual cube.}

\item{chunk_size}{Positive integer. Number of pixels per processing block.}
}
\value{
A list with class \code{"hsi_unmix"}:
\describe{
\item{abundances}{3D array (rows x cols x n_endmembers) of abundance maps.}
\item{residuals}{3D array of reconstruction residuals.}
\item{rmse}{Numeric matrix of per-pixel RMSE.}
\item{endmember_names}{Character vector.}
}
}
\description{
Solves the linear mixing model per pixel using non-negative least squares.
}
\examples{
cube <- hs_example_cube()
# Create simple endmembers
em <- cbind(
  tissue = cube$data[5, 5, ],
  background = cube$data[25, 25, ]
)
result <- hs_unmix_nnls(cube, em)
dim(result$abundances)

}
