% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify.R
\name{hs_classify_svm}
\alias{hs_classify_svm}
\title{SVM Pixel Classification}
\usage{
hs_classify_svm(
  cube,
  training_labels,
  training_mask = NULL,
  kernel = "radial",
  cost = 10,
  gamma = NULL,
  ...
)
}
\arguments{
\item{cube}{An \link{hsi_cube} object.}

\item{training_labels}{Character or factor matrix (same spatial dims as cube)
with class labels. \code{NA} for unlabeled pixels.}

\item{training_mask}{Logical matrix. Alternative to NA in labels. Default \code{NULL}.}

\item{kernel}{Character. SVM kernel: \code{"radial"} (default), \code{"linear"}, \code{"polynomial"}.}

\item{cost}{Numeric. Cost parameter. Default \code{10}.}

\item{gamma}{Numeric. Gamma parameter. Default \code{NULL} (auto).}

\item{...}{Additional arguments passed to \code{\link[e1071:svm]{e1071::svm()}}.}
}
\value{
A list with class \code{"hsi_classification"} containing \code{class_map}.
}
\description{
Trains a Support Vector Machine on labeled pixels and classifies all pixels.
Requires the \code{e1071} package.
}
\examples{
\donttest{
# Requires e1071 package
cube <- hs_example_cube()
labels <- matrix(NA_character_, 30, 30)
labels[1:10, 1:10] <- "class_a"
labels[20:30, 20:30] <- "class_b"
if (requireNamespace("e1071", quietly = TRUE)) {
  result <- hs_classify_svm(cube, labels)
}
}

}
