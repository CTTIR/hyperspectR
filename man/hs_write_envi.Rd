% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io-write.R
\name{hs_write_envi}
\alias{hs_write_envi}
\title{Write an HSI Cube to ENVI Format}
\usage{
hs_write_envi(cube, path, interleave = "bsq", data_type = 4L, verbose = TRUE)
}
\arguments{
\item{cube}{An \link{hsi_cube} object.}

\item{path}{Character. Output path without extension. Files \code{.hdr} and \code{.dat}
will be created.}

\item{interleave}{Character. Data interleave format: \code{"bsq"} (default),
\code{"bil"}, or \code{"bip"}.}

\item{data_type}{Integer. ENVI data type code. Default \code{4} (float32).}

\item{verbose}{Logical. Print progress. Default \code{TRUE}.}
}
\value{
Invisible character vector of written file paths.
}
\description{
Writes an \link{hsi_cube} object as an ENVI header (.hdr) and binary (.dat) file pair.
}
\examples{
cube <- hs_example_cube()
dir <- tempdir()
paths <- hs_write_envi(cube, file.path(dir, "test_cube"), verbose = FALSE)
file.exists(paths)

}
